{
  "name": "layout-lab",
  "version": "0.2.0",
  "private": true,
  "description": "A cross-project, single-file tool to compare candidate UI layouts on real content + real design tokens before implementing them. Exports the layout contract (grid template + CSS vars + Tailwind theme), not finished components.",
  "license": "MIT",
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "serve": "python3 -m http.server 8123"
  },
  "keywords": [
    "ui",
    "layout",
    "design",
    "playground",
    "css-grid",
    "container-queries",
    "design-tokens",
    "prototyping"
  ],
  "engines": {
    "node": ">=20"
  }
}
